# Railway Production Environment Variables
NODE_ENV=production

# CORS Configuration
# Add additional allowed origins separated by commas
# ALLOWED_ORIGINS=https://your-custom-domain.com,https://another-domain.com

# Puppeteer Configuration for Railway
# Puppeteer will use its bundled Chromium
PUPPETEER_CACHE_DIR=/app/.cache/puppeteer

# Performance optimization for Railway
NODE_OPTIONS=--max-old-space-size=512

# Other required environment variables (set in Railway Dashboard)
# SUPABASE_URL=your_supabase_url
# SUPABASE_ANON_KEY=your_supabase_anon_key
# JWT_SECRET=your_jwt_secret
# PORT will be set automatically by Railway