# [DEPRECATED] This file is for Render.com which is no longer used
# Use .env.railway instead for Railway deployment
# Render Production Environment Variables
NODE_ENV=production

# CORS Configuration
# Add additional allowed origins separated by commas
# ALLOWED_ORIGINS=https://your-custom-domain.com,https://another-domain.com

# Puppeteer Configuration for Render
# Puppeteer will use its bundled Chromium
PUPPETEER_CACHE_DIR=/opt/render/.cache/puppeteer

# Performance optimization for Render Free tier
NODE_OPTIONS=--max-old-space-size=512

# Other required environment variables (set in Render Dashboard)
# SUPABASE_URL=your_supabase_url
# SUPABASE_ANON_KEY=your_supabase_anon_key
# JWT_SECRET=your_jwt_secret
# PORT=10000